<header class="site-header">
  <div class="container">
    <div class="header-content">
      <a href="{{ '/' | url }}" class="site-logo">
        <img src="{{ '/assets/images/polaris-logo-horizontal.svg' | url }}" alt="Polaris Advisor" class="logo-lockup" />
      </a>

      <!-- Contact quick link (visible on mobile) -->
      <nav class="site-nav-quick">
        <a href="{{ '/contact/' | url }}">Contact</a>
      </nav>

      <!-- Hamburger button -->
      <button class="nav-toggle" type="button" aria-label="Toggle navigation" aria-expanded="false">
        <span class="hamburger"></span>
      </button>

      <!-- Full navigation (desktop: inline, mobile: slide-out) -->
      <nav class="site-nav-full" aria-label="Main navigation">
        <a href="{{ '/' | url }}">Home</a>
        <a href="{{ '/contact/' | url }}">Contact</a>
      </nav>
    </div>
  </div>
  <div class="nav-overlay"></div>
</header>
